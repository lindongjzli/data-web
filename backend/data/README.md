This directory is intended to hold the raw dataset files.

The `dataset_source` subdirectory should contain all the images and JSON files that you want to be included in the downloadable ZIP archive.
